<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Hannah's Hello Kitty Arcade</title>
  <style>
    :root{--accent:#ff6fa3}
    body{margin:0;font-family:sans-serif;background:linear-gradient(180deg,#fff,#f7f0f8);height:100vh;display:flex;align-items:center;justify-content:center}
    .menu{width:100%;max-width:900px;padding:24px;box-shadow:0 6px 30px rgba(0,0,0,.08);background:#fff;border-radius:12px;text-align:center}
    h1{margin:0 0 8px;color:#333}
    p.lead{margin:0 0 18px;color:#666}
    .choices{display:flex;gap:14px;justify-content:center;flex-wrap:wrap}
  .card{width:260px;text-align:center;cursor:pointer;padding:8px}
  .card img{width:240px;height:160px;object-fit:cover;border-radius:10px;border:4px solid transparent;transition:border-color .12s,transform .12s}
  .card:hover img{transform:translateY(-6px);border-color:rgba(0,0,0,0.06)}
  .card .title{font-weight:700;margin-top:10px}
  .card .desc{color:#666;font-size:13px;margin-top:6px}
    button.back{position:fixed;left:14px;top: 80px;px;padding:10px 14px;border-radius:8px;border:none;background:var(--accent);color:#fff;cursor:pointer;display:none}
    iframe.player{width:100vw;height:100vh;border:0;display:none}
    .small-note{font-size:12px;color:#999;margin-top:12px}
  </style>
</head>
<body>
  <button class="back" id="backBtn">← Back</button>
  <div class="menu" id="menu">
    <div class="choices" aria-hidden="false">
      <button class="card" id="playPong" title="Play Pong" aria-label="Play Hello Kitty Pong">
        <img src="images/pong.png" alt="Pong">
      </button>
      <button class="card" id="playCatcher" title="Play Catcher" aria-label="Play Hello Kitty Catcher">
        <img src="images/bowsbombs.png" alt="Catcher">
      </button>
    </div>
  </div>

  <!-- iframe used to host the selected game. We set src dynamically so returning to menu unloads the game. -->
  <iframe id="playerFrame" class="player" sandbox="allow-same-origin allow-scripts allow-forms"></iframe>

  <script>
    const menu = document.getElementById('menu');
    const backBtn = document.getElementById('backBtn');
    const iframe = document.getElementById('playerFrame');

    function openGame(path){
      // hide menu, show iframe
      menu.style.display = 'none';
      iframe.style.display = 'block';
      backBtn.style.display = 'inline-block';
      // set src -- using relative paths: index.html (pong) and ../hellokittycatcher.html (catcher)
      iframe.src = path;
      // scroll to top to ensure canvas is visible on mobile
      window.scrollTo(0,0);
    }

  document.getElementById('playPong').addEventListener('click', ()=> openGame('index.html'));
  // catcher moved into its own Catcher/ folder
  document.getElementById('playCatcher').addEventListener('click', ()=> openGame('..\/Catcher\/hellokittycatcher.html'));

    backBtn.addEventListener('click', ()=>{
      // unload game by clearing src to stop intervals/animations
      iframe.src = 'about:blank';
      iframe.style.display = 'none';
      backBtn.style.display = 'none';
      menu.style.display = 'block';
      window.scrollTo(0,0);
    });
  </script>
</body>
</html>
